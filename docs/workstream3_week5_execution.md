# Workstream 3 Week 5 Execution

Owner: Team 3 Autonomous Inspection Policy and R2P Evaluation

Week 5 target: learned state-based baseline 0.1.

## Implemented Scope

- Added deterministic state-only feature extraction for policy samples.
- Added a nearest-neighbor behavior-cloning baseline trained from scripted approach and sunshield survey rollouts.
- Added checkpoint hashing, learning-curve generation, compute logging, and failed-run reporting.
- Added learned-policy evaluation against fixed dev episodes with a generated scripted-vs-learned comparison table.
- Kept PPO, image observations, Vast.ai training, and multi-seed sweeps out of Week 5.

## Ship Gates

- Learned baseline 0.1 is available.
- Training run is reproducible from config.
- Learning curve is generated.
- Learned policy evaluates on fixed dev episodes.
- Comparison to scripted baseline exists.
- Training config, seed, checkpoint, and compute metadata are logged.

## Guard Rail Metrics

- Reward is not the final metric.
- Failed runs are reported even when the local run has zero failures.
- Only one seed is used.
- Image observations are disabled.
- Safety zones and coverage regions are not changed.
- Metrics and comparison tables are generated by scripts.
- No GPU result is claimed without synced Vast.ai logs.

## Validation

```powershell
python scripts\validate_contracts.py
python scripts\train_state_baseline.py --config configs\experiments\learned_baseline.yaml --output-dir runs\learned_baseline
python scripts\evaluate_learned_baseline.py --config configs\experiments\learned_baseline.yaml --output-dir runs\learned_baseline
python scripts\run_sunshield_survey.py --config configs\experiments\sunshield_survey.yaml --output-dir runs\sunshield_survey
python scripts\evaluate_thin_slice.py --config configs\experiments\thin_slice.yaml --output-dir runs\thin_slice
python -m unittest discover -s tests
python scripts\validate_run_registry.py
python scripts\e2e_local_smoke.py
```

## Notes

This is a local learned-policy gate, not an official Isaac Sim or Vast.ai training result. The checkpoint is generated under `runs/learned_baseline` and should not be committed; the committed artifact is the deterministic config, code, tests, and execution documentation.
